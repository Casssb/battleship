{
  "name": "battleship",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "sass": "^1.56.0",
    "vite": "^3.2.0",
    "vitest": "^0.24.5"
  },
  "dependencies": {
    "sanitize.css": "^13.0.0"
  }
}
